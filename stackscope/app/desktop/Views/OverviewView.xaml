<UserControl x:Class="StackScope.Desktop.Views.OverviewView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource Brush.Surface.Panel}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Overview" Style="{StaticResource Text.Header}"
                   FontSize="{StaticResource Type.Display}"
                   Foreground="{StaticResource Brush.Text.Primary}"
                   Margin="0,0,0,16"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" MinWidth="180"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition/><RowDefinition/><RowDefinition/><RowDefinition/>
                <RowDefinition/><RowDefinition/><RowDefinition/><RowDefinition/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="Model" Style="{StaticResource Text.Muted}"/>
            <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding ModelName}"
                       Style="{StaticResource Text.Body}"/>

            <TextBlock Grid.Row="1" Grid.Column="0" Text="Architecture" Style="{StaticResource Text.Muted}"/>
            <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding Architecture}"
                       Style="{StaticResource Text.Body}"/>

            <TextBlock Grid.Row="2" Grid.Column="0" Text="Layers" Style="{StaticResource Text.Muted}"/>
            <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding NLayers}"
                       Style="{StaticResource Text.Mono}"/>

            <TextBlock Grid.Row="3" Grid.Column="0" Text="Heads" Style="{StaticResource Text.Muted}"/>
            <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding NHeads}"
                       Style="{StaticResource Text.Mono}"/>

            <TextBlock Grid.Row="4" Grid.Column="0" Text="Hidden" Style="{StaticResource Text.Muted}"/>
            <TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding HiddenSize}"
                       Style="{StaticResource Text.Mono}"/>

            <TextBlock Grid.Row="5" Grid.Column="0" Text="Vocab" Style="{StaticResource Text.Muted}"/>
            <TextBlock Grid.Row="5" Grid.Column="1" Text="{Binding VocabSize}"
                       Style="{StaticResource Text.Mono}"/>

            <TextBlock Grid.Row="6" Grid.Column="0" Text="Transaction" Style="{StaticResource Text.Muted}"/>
            <TextBlock Grid.Row="6" Grid.Column="1" Text="{Binding TransactionId}"
                       Style="{StaticResource Text.Mono}"/>

            <TextBlock Grid.Row="7" Grid.Column="0" Text="Events" Style="{StaticResource Text.Muted}"/>
            <TextBlock Grid.Row="7" Grid.Column="1"
                       Text="{Binding TransactionEventCount, StringFormat={}{0:N0}}"
                       Style="{StaticResource Text.Mono}"/>
        </Grid>
    </Grid>
</UserControl>
